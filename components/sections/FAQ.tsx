/**
 * The original `themedekho-projectstudio-blue.html` does not contain an FAQ
 * section — this file exists to match the requested folder structure and is
 * intentionally a no-op so it doesn't add content that isn't in the source.
 * Drop real markup in here (and render <FAQ /> from app/page.tsx) whenever
 * FAQ content is ready.
 */
export default function FAQ() {
  return null;
}
